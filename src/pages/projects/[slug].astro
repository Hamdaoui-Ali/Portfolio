---
import { getCollection } from "astro:content"
import Layout from "@/layouts/Layout.astro"
import SectionContainer from "@/components/SectionContainer.astro"
import { TECHNOLOGIES } from "@/data/technologies"
import GitHubIcon from "@/components/icons/GitHub.astro"
import ExternalLinkIcon from "@/components/icons/ExternalLink.astro"
import LinkButton from "@/components/LinkButton.astro"
import BeforeAfterMigration from "@/components/BeforeAfterMigration.astro"
import TerminalCard from "@/components/TerminalCard.astro"
import CodeDiffCard from "@/components/CodeDiffCard.astro"

export async function getStaticPaths() {
  const projects = await getCollection("projects")
  return projects.map((project) => ({
    params: { slug: project.slug },
  }))
}

const { slug } = Astro.params
const projects = await getCollection("projects")
const project = projects.find((p) => p.slug === slug)

if (!project) {
  return Astro.redirect("/#projects")
}

const { Content } = await project.render()
const { title, summary, status, role, category, image, github, demo, stack } =
  project.data
---

<Layout
  title={`${title} — Ali Hamdaoui`}
  description={summary}
  image={image}
>
  <SectionContainer class="py-24 md:py-36">
    <nav class="mb-8">
      <a
        href="/#projects"
        class="inline-flex items-center gap-1 text-sm text-gray-500 hover:text-yellow-500 dark:text-gray-400 dark:hover:text-yellow-400 transition-colors"
      >
        ← Back to Projects
      </a>
    </nav>

    <header class="mb-10">
      <div class="flex flex-wrap items-center gap-3 mb-4">
        <span
          class={`rounded-full px-3 py-0.5 text-xs font-medium uppercase tracking-wide ${
            status === "completed"
              ? "bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-400"
              : status === "in-progress"
                ? "bg-yellow-100 text-yellow-800 dark:bg-yellow-900/30 dark:text-yellow-400"
                : "bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-400"
          }`}
        >
          {status === "in-progress" ? "In Progress" : status}
        </span>
        <span
          class="rounded-full bg-gray-100 dark:bg-gray-800 px-3 py-0.5 text-xs text-gray-600 dark:text-gray-400"
        >
          {category}
        </span>
      </div>

      <h1 class="text-3xl md:text-4xl font-bold text-gray-800 dark:text-white">
        {title}
      </h1>

      <p class="mt-3 text-lg text-gray-600 dark:text-gray-400">
        {summary}
      </p>

      <p class="mt-2 text-sm text-gray-500 dark:text-gray-500">
        Role: {role}
      </p>
    </header>

    <div class="mb-10">
      <h2 class="text-lg font-semibold text-gray-800 dark:text-white mb-3">
        Technology Stack
      </h2>
      <ul class="flex flex-row flex-wrap gap-2">
        {stack.map((key) => {
          const tech = TECHNOLOGIES[key]
          return (
            <li>
              <span
                class={`flex gap-x-2 rounded-full text-xs ${tech?.class ?? "bg-gray-500 text-white"} py-1 px-2`}
              >
                {tech?.name ?? key}
              </span>
            </li>
          )
        })}
      </ul>
    </div>

    {
      (github || demo) && (
        <div class="flex items-center gap-4 mb-10">
          {github && (
            <LinkButton href={github}>
              <GitHubIcon class="size-5" />
              Source Code
            </LinkButton>
          )}
          {demo && (
            <LinkButton href={demo}>
              <ExternalLinkIcon class="size-4" />
              Live Demo
            </LinkButton>
          )}
        </div>
      )
    }

    {
      slug === "ai-migration-factory" && (
        <BeforeAfterMigration
          context="Migration example: modernizing a Java + Spring Boot application"
          beforeTitle="Legacy Stack"
          beforeItems={[
            "Java 8 runtime",
            "Spring Boot 2.x",
            "JUnit 4 test framework",
            "XML-based configuration",
            "Legacy dependency versions with known CVEs",
          ]}
          afterTitle="Modernized Stack"
          afterItems={[
            "Java 21 LTS runtime",
            "Spring Boot 3.x with native compilation support",
            "JUnit 5 with parameterized test support",
            "Java-based configuration with auto-configuration",
            "Updated dependencies with security patches applied",
          ]}
        />
        <CodeDiffCard
          title="Spring Boot Upgrade Example"
          context="Automated dependency and configuration transformation"
          beforeCode={`<parent>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-parent</artifactId>
  <version>2.7.18</version>
</parent>

<dependency>
  <groupId>junit</groupId>
  <artifactId>junit</artifactId>
  <version>4.13.2</version>
</dependency>`}
          afterCode={`<parent>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-parent</artifactId>
  <version>3.3.0</version>
</parent>

<dependency>
  <groupId>org.junit.jupiter</groupId>
  <artifactId>junit-jupiter</artifactId>
  <version>5.10.2</version>
</dependency>`}
        />
      )
    }

    {
      slug === "personal-portfolio" && (
        <TerminalCard
          title="CI/CD — quality workflow"
          command="pnpm install --frozen-lockfile && pnpm check && pnpm build"
          output="✓ 0 errors\n✓ 0 warnings\n✓ 12 pages built in 4.2s"
        />
      )
    }

    <article
      class="prose prose-gray dark:prose-invert max-w-none
        prose-headings:text-gray-800 dark:prose-headings:text-white
        prose-p:text-gray-700 dark:prose-p:text-gray-300
        prose-strong:text-gray-800 dark:prose-strong:text-white
        prose-li:text-gray-700 dark:prose-li:text-gray-300
        prose-a:text-yellow-500 dark:prose-a:text-yellow-400"
    >
      <Content />
    </article>
  </SectionContainer>
</Layout>
